// Toggle for the MisterPrada-inspired particle-morph hero experiment.
// true  -> renders MorphParticlesExperience (src/components/hero-experiment)
// false -> renders the production HeroScene (src/components/hero) unchanged
//
// Flip this back to false (or delete src/components/hero-experiment and this
// file) to fully revert to the original hero with no other code changes.
export const USE_MORPH_PARTICLES_EXPERIMENT = true;
